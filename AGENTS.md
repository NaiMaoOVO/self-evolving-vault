# AGENTS.md — 自进化知识库工程入口

> 施工教材：`教程/` 目录（以 `教程/00-总导航.md` 为入口）。
> 施工规则：每次只执行一章；上一章验收未通过不得进入下一章；任何外发、secret写入、删除、批量移动和数据库迁移均需用户单独授权。

## 工程布局

| 路径 | 职责 | 权限 |
| --- | --- | --- |
| `教程/` | 施工教材（只读） | 禁止修改 |
| `施工记录/` | 各章验收记录（PASS/FAIL） | 可写，追加式 |
| `vault/` | Obsidian vault，知识库本体 | 见 vault/AGENTS.md |
| `系统/` | 自动化脚本、适配器（第03章适配器、第08章起状态机） | 按章授权 |

## 多智能体协作规范

- **主智能体（统筹）**：读总导航 → 检查前一章验收记录 → 派发子智能体施工 → 验收子智能体产出 → 生成验收记录。不直接写业务文件。
- **施工子智能体**：只领一章。开工前报告输入、精确写入路径、外部通信和风险；按"需求→设计→实现→测试"交付；不跨章、不改教材。
- **验证子智能体**：独立于施工者。只读审计 + 受控测试，输出证据和 PASS/CONDITIONAL PASS/FAIL。

## 全局安全边界（任何智能体必须遵守）

1. 不输出 secret；secret 只写环境变量或被 git 忽略的本地配置。
2. 不覆盖、不移动、不删除 `vault/00_资源库/` 原始资料。
3. 外发（网络上传、邮件、发布）、删除、批量改名、数据库迁移：一律先向用户请求授权。
4. 测试必须验证内容和真实行为，不能只看命令退出码。
5. FAIL 时停止并留下可恢复状态，不伪造通过。

## 当前进度

见 `施工记录/进度.md`。
