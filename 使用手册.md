# 使用手册 —— 打开并运转你的自进化知识库

> 面向仓库主人（新用户）的日常操作说明。所有命令均在本机实测过。

## 0. 三分钟理解你的系统

一句话：**外部资料进来 → 有证据地蒸馏成知识 → 产出真实成果 → 你的反馈驱动规则升级 → 下一轮产出自动变好。**

五层各司其职（都在 vault/ 下）：

| 层 | 放什么 | 铁律 |
| --- | --- | --- |
| 00_资源库 | 原始资料卡（外部抓取+你的历史资料） | **只读**，永不覆盖删除 |
| 01_主题Wiki | 知识卡（事实区/判断区严格分离） | 每条知识必须能回溯来源 |
| 02_项目库 | 项目卡、决策卡 | 决策可逆性必须标注 |
| 03_输出库 | 正式成果（报告/方案/工具） | done 必须对照完成定义 |
| 04_系统维护 | 规则画像、反馈卡、提案、结案、运行记录 | 系统的大脑与日志 |

分工：**机械活归系统与 agent，你只做三件事——定目标、给反馈、做批准。**

## 1. 打开项目的三个入口

- **入口 A · 日常阅读与编辑**：用 Obsidian 的「打开文件夹作为仓库」选择 vault/ 目录。
- **入口 B · 终端运维**：cd 到项目根目录，本手册所有命令都在那里执行。
- **入口 C · AI 协作**：在项目根目录启动你的 AI 编程会话，它会自动读取 AGENTS.md 的边界规则——涉及时先查 GBrain 再回答。

## 2. 每天的三种典型用法

### 场景 A · 跑一轮每日生产

    python3 系统/state_machine/runner.py run      # 每日轮
    python3 系统/state_machine/runner.py status   # 看当前状态
    python3 系统/state_machine/runner.py resume   # 中断后续跑

- 今天已成功过一轮？再跑会显示「R7 跳过」——这是防重复污染的特性，不是故障。
- 机械轮只做到 mechanical_ok；正式的蒸馏、结案由你的 AI 会话接力完成。

### 场景 B · 查记忆（brain-first）

在你的 AI 会话里直接说：「先查 gbrain 再回答 XXX」。终端方式：

    export PATH="$HOME/.bun/bin:$PATH"
    gbrain search "关键词"
    gbrain query "自然语言问题"

⚠️ **锁规则（重要）**：GBrain 的 MCP 服务与 CLI 互斥——AI 客户端连着 GBrain 时，CLI 报 Timed out waiting for PGLite lock 属正常。此时请在 AI 会话里用 MCP 工具查询；要用 CLI 就先断开客户端连接。

### 场景 C · 对成果不满（反馈→进化，系统的灵魂）

1. 直接对 AI 说出反馈原话（例：「建议太笼统没有数字门槛」），它会把原话存成反馈卡；
2. **同一意见 ≥2 次，或带量化结果数据**，周进化才会升级为提案——一次意见不许撼动全局；
3. 提案需**你明确批准**后 apply，规则 version+1；
4. 之后同类任务的新产出自动避开旧问题（前后对照留档）。

## 3. 每周一次：进化评审

    python3 系统/weekly_evolution.py review   # 聚类近7天反馈信号

有达标信号按模板写提案 → 你批准 → apply。无合格信号会如实输出「本周无可验证进化」。

## 4. 健康检查速查

    python3 系统/quality_gate.py check        # 15项闸门，退出码0=健康
    cat vault/04_系统维护/状态.md             # 库状态一览
    git push                                  # 同步 GitHub 异地备份

判读：PASS=正常；WARN=提醒审计（不阻断）；FAIL=违反铁律必须处理；N/A=环境暂不可取证。

## 5. 安全边界（必读）

- **外发**（上传/发布/邮件）、**删除**、批量移动、数据库迁移——任何 agent 都必须先征得你同意。
- 00_资源库/ 对所有自动化只读。
- API key 等 secret 只放环境变量，绝不进文件入库。

## 6. FAQ

- Q：gbrain 报 PGLite lock？ 见场景 B 锁规则；服务随 AI 客户端自动拉起，无需手动管理。
- Q：runner 说「跳过」？ R7：24 小时内同目标日已有合法结案，不重复开轮。
- Q：怎么备份？ git push 即可；本地 Markdown 永远是真相源。
- Q：想加新 RSS 源？ 让 AI 会话按第03章协议注册，抓取失败会如实记录。
