# 测试-增量

- **用途**：第02章 GBrain 增量刷新验证（无敏感信息）。本文件在首次全量导入之后创建，用于验证增量 import。
- **创建时间**：2026-08-21（首次导入后）

## 增量验证句

增量刷新验证句子-ZEPHYR-7749。

## 预期

- 首次全量导入（5 个文件）之后创建本文件。
- 执行 `gbrain import <vault> --no-embed` 增量导入后，检索 ZEPHYR-7749 必须命中本文件，且 brain pages 数变为 6。
- 本文件保留在 vault 中，供后续章节（跨会话 MCP 检索等）复验使用。
