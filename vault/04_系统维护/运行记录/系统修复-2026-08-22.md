# 系统修复记录（2026-08-22）

- 执行者：施工子智能体（用户已授权全部三项修复）
- 范围：① 删除5张重复外部卡 ② 适配器幂等缺陷修复 ③ gbrain 升级 0.46.25.0
- 未动：9+8 张正常卡内容、教程/、施工记录/、工程根文件；无外发

## 任务1：删除5张重复外部卡

**授权依据**：用户授权（2026-08-22），清单见 [[第07章-冻结清单-2026-08-22]]。

**核对**（删除前实测，非仅凭清单）：扫 `00_资源库/外部知识/` 全部22张卡 frontmatter `id` + `url`，确认5对 id+URL 完全相同：

| id | 保留（存量） | 删除（重复，标题译文差异） |
| --- | --- | --- |
| 21b220d0ec1e | Matt-Webb-用-ChatGPT-学习四元数开发-AR-应用 | Matt-Webb-用-ChatGPT-当导师学会四元数 |
| 4671a34b716a | 意外劫持-E164-ARPA电话路由漏洞揭示 | 意外劫持-e164arpa-并记录军事基地通话 |
| 51b0cf062317 | 停止制作-TUI编码代理让原生-UI-成本骤降 | 停止制作-TUI原生-UI-成本已大幅降低 |
| dc6f0a5929be | Kagi-新增过滤付费墙链接的搜索设置 | Kagi-新增过滤付费墙链接设置 |
| e4fca15e2e69 | 我们正生活在赛博朋克式的未来中 | 现实与赛博朋克我们生活在吉布森的未来中 |

**备份**：删除前将5张卡完整原文（含 frontmatter，共21,038字节）写入 [[已删除重复卡备份-2026-08-22]]，任何一张可据此完整恢复。

**删除后核验**：目录 22 → **17** 张（=9存量+8新卡；任务书"9+8−5=12"系算术笔误，5张重复是13条日报入库中产生，实测以17为准）；`sort \| uniq -d` 复扫 id 重复数 = 0。

**GBrain 刷新**：`gbrain import vault --no-embed` → 64/64 扫描，2 pages imported。
**pages 轨迹：63 → 64**。解释：被删5张在 brain 层本已按 frontmatter id 合并（不占独立 page），删除后 pages 不减；新增备份文件 +1 page。刷新后 64 md ↔ 64 pages 一一对应、零合并，比删前（68 md → 63 pages）状态更干净。

## 任务2：适配器幂等缺陷修复（系统/horizon_adapter.py）

**根因定位**：`load_existing_ids()` 用正则 `^id:\s*(\S+)` 捕获存量卡 frontmatter id，但卡内 id 带双引号（`id: "21b220d0ec1e"`），捕获值含引号；而 `item_id()` 生成的判重 key 不带引号 → 集合比对永不相等 → **id 判重形同虚设**，实际只剩 `target.exists()` 文件名判重生效。标题译文差异 → slug 不同 → 文件名不同 → 同 URL 重复入库。

**修复 diff**（1行）：

```diff
     m = re.search(r"^id:\s*(\S+)", head, re.MULTILINE)
     if m:
-        ids.add(m.group(1))
+        ids.add(m.group(1).strip("\"'"))
```

文件名冲突但 id 不同时加 `-<id前6位>` 后缀规避的原有逻辑（写入前 `if target.exists()` 分支）保留不变。

**回归测试**（真实数据：本轮隔离日报 run-07 全13条目 + 当前17卡集合拷贝至临时 regress-vault 副本，`--no-fetch`）：

| 运行 | 条目 | 新入库 | 跳过 | 结论 |
| --- | --- | --- | --- | --- |
| 修复前 dry-run | 13 | **5** | 8 | 缺陷复现：5条已存在 id 的 URL 被判"新"（与上轮重复入库行为一致） |
| 修复后 dry-run | 13 | **0** | **13** | 幂等生效 |
| 修复后真实写入 | 13 | **0** | **13** | 副本目录保持17张，零写入，仅生成运行报告 |

## 任务3：gbrain 升级 0.46.24.0 → 0.46.25.0

- **备份先行**：`cp -R ~/.gbrain ~/.gbrain.backup-20260822`，大小 43M（唯一跳过项为活动 socket，运行时产物非数据）
- **升级**：`bun install -g github:garrytan/gbrain`（main HEAD #7e3ce95 版本号未 bump）→ 改用显式 tag `#v0.46.25.0`；途中遇 bun `DependencyLoop`，`bun remove -g gbrain` 后重装成功
- **版本**：升级前 0.46.24.0 → 升级后 **0.46.25.0**（`gbrain --version` 实测）
- **doctor 关键项**：db_checks（connection / pgvector / schema_version / rls / jsonb_integrity 等全部检查项）跑完无中断；`embedding_migration_state: no embedding migration in flight` → **无迁移需求，未执行迁移，未触发回滚**
- **数据完好**：升级后 pages=64 保持，import 幂等（0 imported / 64 unchanged / 0 errors）

**遗留 WARN/FAIL（与本次升级无关，登记待办）**：
1. `sync_freshness [FAIL]`：evolving-kb 源从未跑过 `gbrain sync`（图/backlinks 计算，历史状态非升级引入）
2. `provider_sunset [WARN]`：embedding 模型 zeroentropyai:zembed-1 的 hosted API 2026-09-04 关停。当前库 embedding=禁用（import --no-embed）不受即时影响；若未来启用语义检索需先决策（自托管 zembed-1 或 `gbrain migrate embeddings --to ...`），涉及成本/密钥，待用户授权

## 最终一致性

- vault md：**64**（68 − 5删卡 + 1备份文件）
- brain pages：**64**（轨迹 63→64，见任务1）
- 幂等复核：二次 import 0 imported / 64 skipped
