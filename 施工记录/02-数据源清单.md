# 02-数据源清单

- 注册/导入日期：2026-08-21
- 施工者：第02章施工子智能体（第二阶段）

## 注册的数据源

| source id | 名称 | 路径 | federated | pages | 注册命令 |
| --- | --- | --- | --- | --- | --- |
| evolving-kb | evolving-kb | `/Users/chenzixun/Documents/自进化知识库/vault` | 是（默认搜索可见） | 6（截至 2026-08-21） | `gbrain sources add evolving-kb --path <vault绝对路径> --name evolving-kb --force` |

- `--force` 原因：`sources add` 默认要求路径是**已提交文件的 git repo**；工程根虽是 git 仓库但尚无任何 commit（vault 未被跟踪），且本阶段禁止执行 git 写操作，故使用官方提供的 `--force` 跳过该检查。
- 注册后执行 `gbrain sources federate evolving-kb`，使其进入跨源默认搜索（默认为 false，只在显式 `--source` 时可见）。
- 另有 gbrain 内置 `default` 源（0 pages，无 local_path），未使用。
- `import` 自动路由：brain 中存在唯一非默认源时，`gbrain import` 自动路由到 evolving-kb（输出有明确提示）。

## 导入记录

| 批次 | 时间（依据文件 mtime 推定） | 文件数 | 结果 |
| --- | --- | --- | --- |
| 全量导入 | 2026-08-21 16:42–16:44 | 5 | 5 pages imported，5 chunks，0 skipped，0 errors（0.2s） |
| 增量导入 | 2026-08-21 16:46 前后 | 6（1 新增） | 1 page imported，5 unchanged skipped，0 errors（0.2s） |

命令：`gbrain import "/Users/chenzixun/Documents/自进化知识库/vault" --no-embed`

## 已导入文件（6）

| 文件 | 说明 |
| --- | --- |
| AGENTS.md | vault 级规则（第01章已有） |
| 安装验证.md | 第01章验收测试句（第01章已有） |
| 测试-精确关键词.md | 第02章测试数据（含"量子苔藓在低温环境下表现出超导特征"） |
| 测试-同义语义.md | 第02章测试数据（无重叠词的同义句） |
| 测试-冲突陈述.md | 第02章测试数据（两条带日期的冲突陈述） |
| 测试-增量.md | 第02章增量刷新验证（含"增量刷新验证句子-ZEPHYR-7749"），**保留**供后续章节复验 |

## 一致性基线（2026-08-22 复核）

- vault Markdown 文件数：6
- `gbrain stats`：Pages 6，Chunks 6（另有 2 个已软删除探针 chunk 残留计数，见 `02-安装验收记录.md` 路径变更探针一节），Embedded 0
- 注意：`sources status` 的 "LAST SYNC: never" 指 git 型 `gbrain sync` 从未运行，与 import 无关（本工程刷新走 import 路线，见 `02-刷新协议.md`）。
