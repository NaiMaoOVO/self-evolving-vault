# 第九章独立验证报告（反馈自进化与质量闸门）

> 验证子智能体：第九章·独立验证（未参与施工，只读审计+受控复测）
> 验证时间：2026-08-22T20:04–20:15 (+08:00)
> 验证对象：系统/quality_gate.py、系统/weekly_evolution.py、第09章落库文档与 vault 产物
> 方法：教材逐条映射 + 独立复跑 + 自建坏样本注入（未复用施工方样本）+ 安全边界核查
> 施工方报告（施工记录/09-施工报告.md）仅作线索，不作为凭据。

## 总判定：**PASS**

全部必做核查项 A–G 通过；2 项受环境限制无法直接验证（见"无法验证项"），均有替代证据或预期 N/A 取证；4 条最小修复建议均为文档/注释级，不影响闸门功能与安全性。

---

## A. 教材映射 —— PASS

**14 条闸门 ↔ 15 个 check 函数逐条映射**（依据 quality_gate.py L113–629 实读）：

| 教材闸门 | 对应检查 | 判定 |
| --- | --- | --- |
| 1 资源层零覆盖 | check_01（git diff M/D/R/C/T→FAIL，非git→N/A） | ✓ |
| 2 secret扫描 | check_02（sk-/api_key/password/token 正则+占位排除） | ✓ |
| 3 来源存在、日期有效 | **拆分**：check_03 + check_04 | ✓ |
| 4 事实与判断分离 | check_05 | ✓ |
| 5 不虚构实测/经历/引用 | check_06 | ✓ |
| 6 不重复生产旧成果 | check_07 | ✓ |
| 7 每轮唯一结案 | check_08（(日,轮)分组查重） | ✓ |
| 8 抓取失败≠无新增 | check_09 | ✓ |
| 9 GBrain页面/索引/embedding一致 | check_10（三维度；锁拒→N/A留证） | ✓ |
| 10 状态文件一致 | check_11（三方比对） | ✓ |
| 11 中间页不冒充成果 | check_12 | ✓ |
| 12 用户目标质量高于技术通过 | check_15 | ✓ |
| 13 连续低分/重复否决触发暂停 | check_13 | ✓ |
| 14 画像长期无更新触发审计 | check_14（>30天 WARN 不 FAIL） | ✓ |

覆盖完整、无遗漏、无重复。行数属实：quality_gate.py=665、weekly_evolution.py=327、说明文档=57（wc -l 核验）；ast.parse 双脚本语法通过（Python 3.9.6，未用 py_compile）。

**两提示词落库一致性**：
- 周进化提示词.md L20–29 与教程 L16 八条正文**逐字一致**；七步流程与铁律自检清单为可执行细化，口径未偏离。
- 反馈处理提示词.md L20–28 五维拆解与教程 L10 口径一致（"正反馈/负反馈/缺口/结果数据"落为"类别：…"，语义等价）；铁律句"除事实错误和安全问题外，一次反馈不得直接改全局画像"逐字保留于 L16/L28。

**进化协议与铁律完备**：进化协议.md 四条铁律 = 单次不改全局（一）/≥2 重复或结果数据才升级（二）/无行为差异不标 evolved（三）/无信号写"本周无可验证进化"（四），每条附执行点；L56 relations 已挂链两张提示词卡。反馈卡模板含 原话/对象/范围/强度/动作/事实/判断 分区，真实反馈卡结构与之相符。

微瑕（非阻断，见修复清单①②③）：quality_gate.py L113 注释写"14 项检查"实为 15；说明文档 L10/L25 "14 项"措辞与 15 行表格不一致；docstring "新增⑮"表述易误读（⑮实为教材第 12 条闸门，非超出 14 条之外的新增）。

## B. T1 独立复跑（真实 vault）—— PASS

```
命令：python3 系统/quality_gate.py check   # 2026-08-22T20:04:35+08:00
汇总：PASS=14 FAIL=0 WARN=1 N/A=0；退出码 0（实测 EXIT_CODE=0）
[WARN] 10 GBrain一致性：页面数 vault实际.md=73 vs vault_md_count=65 / brain_pages=65，差8>2 → 待刷新
      索引/embedding: N/A —— gbrain serve(PID 34462) 持 PGLite 锁，CLI 被拒(exit=1)，stderr 摘录已固化
```

数值独立核验：`find vault -name "*.md" -not -path "*/.obsidian/*" | wc -l` = **73** ✓；状态.md L24–25 `vault_md_count: 65` / `brain_pages: 65` ✓。vmc==bp 故不构成 FAIL；差异>2 判 WARN，定性"待刷新非缺陷"正确——差额含本章新增（真实反馈卡×1、提示词卡×2）及第07章后新增 wiki 卡，状态刷新按协议归第02/08章管辖。注：任务书预期"72 vs 65"为施工 T1 时点值，T5 落库反馈卡后即 73 vs 65，与施工报告第五节第 2 条自述一致。

## C. 自建坏样本注入（8 类全命中）—— PASS

环境：`/tmp/qg9-audit-c1/vault`（本验证自建，样本命名/日期/格式均未复用施工方 T2 样本；验证后已删除）。
结果：**PASS=3 FAIL=8 WARN=1 N/A=3，退出码 1**，每项 FAIL 理由与注入内容一一对应：

| 注入内容 | 命中检查 | 实际输出摘录 |
| --- | --- | --- |
| `password = Xk9mQ2vLp8zR4w2026`（构造会命中的长值格式，行内无占位词） | ②secret扫描 FAIL | `秘密泄漏样本.md:10: password = Xk9…` |
| `[[消失的引用源-QA77]]` 断链 | ③来源存在 FAIL | `断链卡.md → [[消失的引用源-QA77]] 目标不存在` |
| `created: 2026/08/26` 非法日期 | ④日期有效 FAIL | `created='2026/08/26' 非合法 ISO` |
| 知识卡仅有 ## 事实 无 ## 判断 | ⑤事实判断分离 FAIL | `缺判断区卡.md 缺 判断 区` |
| 结案-2026-08-26-轮次1.md 与 -轮次1-补充.md 同日同轮双份 | ⑧唯一结案 FAIL | `2026-08-26 轮次1 ×2` |
| 03_输出库 文件 status=draft | ⑫draft冒充 FAIL | `中间稿冒充成果.md status=draft` |
| 两张否决反馈卡 + 状态机.json paused=false | ⑬否决暂停 FAIL | `否决信号 2 条但 paused=false` |
| 结案类型A 仅凭"测试通过，全部检查绿灯"，全篇无用户目标证据 | ⑮用户目标 FAIL | `技术通过冒充用户目标达成` |

对照组合格卡未被误报；check_01/09/14 因目录缺失按设计 N/A。假 secret 采用 password= 长值格式（sk-TEST 开头不计入），实测确实触发正则命中。

## D. 单次反馈不污染 与 真实 review —— PASS

**D1 隔离复测**（/tmp/qg9-audit-d3，已删）：预置 version:3 规则文件 + 1 条单次无数据合成卡 →
```
review 输出：强度判定=只记录/候选（n=1，结果数据=否）
             本周无可验证进化：仅单次反馈/候选信号，未达 ≥2 重复或结果数据阈值。
规则 shasum 前=后=750e56f8acae7e…（不变）；退出码 0
```

**D2 真实 vault 复跑**：命中 1 张真实反馈卡（反馈-2026-08-22-结案记录模板.md）→ n=1 只记录/候选 → 结论"本周无可验证进化：仅单次反馈/候选信号…"；规则与画像+反馈与进化两目录聚合 sha256 前=后=038ccf5919de…（review 零写入实证）；退出码 0。

## E. 施工报告证据链抽查 —— PASS

**① 日期修复前后原文 vs 现状**（vault/01_主题Wiki/AI工具与效率/编码代理与原生UI成本.md）：
- 现状 frontmatter `updated: 2026-08-22`（合法 ISO，无中文注记）；正文 H1 后 L14 存在 `> 更新注记：2026-08-22（第07章轮次1：追加 Bun 1.4 WebView 证据）`，注记原文未丢失。
- git diff HEAD 显示：updated 行与 HEAD 一致（修复回合法原值故不出现在 diff 中），注记行为新增行——与施工报告 T1 修复记录的"修复前/修复后/注记迁移"三段描述完全吻合。该 diff 中其余增改（source 字段、Bun 1.4 事实、证据链接、relations）属第07章既定工作树变更，与本章修复声明不冲突。

**② T6 文本自洽性 + 同法复现核心断言**：
- T6 小节文本自洽：环境/注入/断言/清理四环节闭环，且声明 /tmp/qg-t6 已清理（实测 `/tmp/qg-t2.* /tmp/qg-t3* /tmp/qg-t6` 均 No such file or directory，清理属实）。历史时点 shasum（97b4f8…/427b86…）不可重放，列入"无法验证项"。
- 本验证以同法在 /tmp/qg9-audit-t6x 独立复现（已删）：两条同(对象,范围)模拟反馈卡 → review 判「可提稳定规则」且聚类 n=2 → apply --approved 后目标规则 version **1→2**、提案 status open→accepted、退出码 0；`diff -u` 规则文件**仅一行变化 `-version: 1 / +version: 2`**。核心断言全部复现成立。

## F. 安全边界 —— PASS（crontab 一项受限，见无法验证项）

- **00_资源库零改动**：`git status --porcelain -- vault/00_资源库` 仅 8 条 `??`（untracked 新增，系第03章收件箱分拣产物，check_01 设计允许），**无 M/D/R 行，无删除** ✓。
- **真实反馈卡**（vault/04_系统维护/反馈与进化/反馈卡/反馈-2026-08-22-结案记录模板.md）：原话区程序化逐字比对 = **True**（「第07章的结案记录模板字段太多，我每次都要填十几项，实际只关心结论和清单」，一字不差）；frontmatter `status: new` ✓；强度区如实标注 medium 并显式声明"『每次填写』系使用频次描述，非同信号重复反馈次数"，无人为拔高 ✓。
- **归属佐证**：状态.md(mtime 11:08)、状态机.json(mtime 10:49) 的修改时间均早于第九章施工时段（约18:00 后），与施工方"仅读取未修改"声明相符；进化协议.md(mtime 18:44) 属其声明内的 relations 挂链修改。
- crontab：`crontab -l` 被沙箱拒绝（Operation not permitted, exit 126），/usr/lib/cron/tabs 与 /var/at/tabs 亦 Permission denied → 无法直接验证（见下）。替代观察：~/Library/LaunchAgents 8 个 plist 均为无关第三方应用，无本项目相关定时任务。

## G. 终态复核 —— PASS

- 真实反馈卡保持 `status: new` 候选留观，动作区 L33 记录 review 判定（只记录/候选，n=1，未达铁律二门槛，暂不建提案留观），**未被升级**、未据其修改任何规则文件（D2 聚合 sha 不变佐证）✓。
- 进化提案目录 `find` 实测仅 README.md，排除 README 后提案文件数 = **0**，无未授权提案 ✓。

---

## 无法验证项清单

1. **crontab -l 为空**：沙箱拒绝执行 crontab 与读取 tabs 目录，无直接证据；仅有 LaunchAgents 无关任务清单 + 施工报告声明的间接证据。
2. **check_10 索引/embedding 维度**：gbrain serve(PID 34462) 持 PGLite 锁，CLI 全部被拒——环境既定事实，按任务书预期报 N/A，取证已固化在 B 项输出中（绝不 kill 进程）。
3. **施工报告历史时点哈希**（T3 规则 sha 427b86…、T6 聚合 sha 97b4f8…）与 T2 历史输出：测试环境已按授权清理，不可重放；其中每类断言已经由本验证 C/D/E② 以同法独立等价复现。

## 最小修复清单（均为非阻断建议）

1. 系统/quality_gate.py L113 注释 `# ---------- 14 项检查 ----------` 改为 15 项（纯注释，零逻辑影响）。
2. 系统/第09章-质量闸门说明.md L10 "跑全部 14 项"、L25 "## 14 项检查方法" 改为 15 项，消除与其 15 行表格的计数矛盾。
3. （可选）quality_gate.py docstring L8 "新增⑮用户目标质量高于技术通过" 建议改为 "教材第12条闸门落位于⑮"，避免误读为超出教材 14 条之外的扩充。
4. 状态.md `vault_md_count` 待按第02章刷新协议/第08章管辖刷新至当前实际值（73），消除 check_10 WARN——需另行授权，非本章缺陷。

## 验证过程遗留物声明

- 新建交付物仅本文件（施工记录/09-独立验证报告.md）。
- 验证用临时物（/tmp/qg9-audit-c1、/tmp/qg9-audit-d3、/tmp/qg9-audit-t6x、.tmp-verify-t6x.sh、.tmp-verify-fg.sh）均已由本验证自行创建并自行删除，已复核清除。
- 未修改真实 vault 任何文件；未执行 git 写、外发、crontab、进程 kill；gbrain 仅经 quality_gate 内建探测被动取证一次。
