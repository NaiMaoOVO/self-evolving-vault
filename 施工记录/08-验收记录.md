# 第08章验收记录（自动运行状态机与故障恢复）

- 验证者：独立验证子智能体（未参与施工）
- 日期：2026-08-22
- 方式：只读审计 + 受控复测（隔离目录 `/var/folders/…/T/opencode/sm-verify/` 已创建并在收尾删除；真实 vault 仅执行安全的 R7 skip 复测；git 仅 status 只读；无外发、无 git 写操作、未修改 vault/系统 任何文件）
- 输入：教程/08-自动运行状态机与故障恢复.md + 施工方声明（runner.py 966 行 + DESIGN.md + README.md，6 子命令，9 状态机，flock 锁，故障注入 A-F + R4）
- git 基线：`be7d1e0`（第01-04章完成；其后第05/07/08章均未提交，故 git status 混合多章改动，逐项甄别）
- 教程通过标准：同一输入最多一个合法结案 / 中断恢复不重复 / 时间滞后与真实失败分开 / mechanical_ok 不冒充正式成功

## 一、逐项审计结果

### 1. 代码存在与可执行 — PASS

- `ls 系统/state_machine/`：runner.py（36966 B）+ DESIGN.md（13005 B）+ README.md（7600 B）三件齐全。
- `python3 系统/state_machine/runner.py --help` 显示 6 子命令：`run / force / resume / status / pause / uninstall`，与声明一致。
- runner.py 实测 966 行，纯标准库（argparse/datetime/hashlib/json/os/random/re/shlex/subprocess/sys/time/pathlib/zoneinfo），无第三方依赖。

### 2. 9 状态完整 — PASS

教程列出 9 状态，runner.py 全部覆盖（自动层只驱动至 mechanical_ok，下游 5 态由 agent/用户推进——符合教程"mechanical_ok 不冒充正式成功"）：

| 状态 | 自动层驱动 | 代码证据 |
| --- | --- | --- |
| idle | ✅ fresh_state 默认 | runner.py:130 / should_skip |
| running | ✅ start_run 设置 | runner.py:753 |
| mechanical_ok | ✅ finalize_ok 设置 | runner.py:759 / render_md_bullet:270 |
| failed | ✅ finalize_failed 设置 | runner.py:777 / same_day 续跑:819 |
| draft_pending | 识别（不重开轮） | should_skip:694 |
| promoted | 识别 | should_skip:694 |
| closed_knowledge | 识别 | should_skip:694 |
| closed_drop | 识别 | should_skip:694 |
| evolved | 识别 | should_skip:694 |

DESIGN.md §2.1 图示与教程状态表一一对应；下游 5 态在 `should_skip` 被识别（同目标日已有成功轮次则跳过），不与 agent 接力冲突。

### 3. 单实例锁复测（D）— PASS（隔离复测）

隔离环境起慢实例（`SM_HORIZON_CMD=horizon-slow` sleep 20）持锁，3s 后起第二实例：

```
instance2 exit_code=3（期望 3，EXIT_LOCKED）
输出：单实例锁：另一实例运行中：run_id=sm-20260822T024756Z-68d3 pid=10817（flock 持有中；若确认僵死可 kill 后重试）
日志：[锁] 拒绝启动：另一实例运行中…
```

flock `LOCK_EX|LOCK_NB` 内核级互斥，第二实例立即退出码 3 并报告对方 run_id/pid。实现为 flock（非设计文的 O_EXCL），DESIGN 偏差记录已登记为正向偏差（进程死亡内核自动释放，无 TOCTOU）。

### 4. R7 skip 复测 — PASS（真实环境复测）

真实环境执行 `force --date 2026-08-22`：

| 指标 | 复测前 | 复测后 | 判定 |
| --- | --- | --- | --- |
| 结案文件数 | 1 | 1 | 不产生新结案 ✅ |
| 状态机 state | idle | idle | 不重复 ✅ |
| 状态机日志数 | 2 | 3（+1 skip 日志） | 仅记跳过 ✅ |

输出：`[调度] 跳过：24h内同目标日已有合法结案：结案-2026-08-22-轮次1.md（R7 跳过）`，退出码 0。skip 路径在任何阶段前返回，不触发 Horizon/适配器，安全。

### 5. 错过补跑复测（F）— PASS（隔离复测，附说明）

**说明**：真实 `状态机.json` 的 `last_success=null`（历次真实运行均命中 R7 skip，未产生 mechanical 成功），故真实环境无法计算 gap、无补跑语义可测；且对真实 vault 跑 `--date 2026-08-21` 会触发真实 Horizon 摄取（网络+写 vault），违反只读审计边界。故在隔离环境伪造 `last_success=2026-08-19`（3 天前）、`SM_NOW_ISO=2026-08-22` 复测，忠实模拟补跑场景。

复测结果：
```
[调度] 补跑：距上次成功(2026-08-19) 3 天，合并错过的天数为一轮，窗口=73+2=75h（只补一次，非故障）
```
- 窗口自动覆盖 gap（73h+2 缓冲=75h）✅
- `pending_backfill={"merged_days":3}` 正确登记 ✅
- 全程零"故障"字样（唯一"故障"出现在"**非**故障"短语中，显式否定）✅
- 各阶段完成 → mechanical_ok（退出码 0）✅
- 二次 run：`[调度] 跳过：今日机械轮已完成（mechanical_ok），待 agent 蒸馏接力，不重复开轮`，cards 不翻倍 ✅

时间滞后与真实失败被正确分开（R14）。

### 6. 恢复点机制审查 — PASS（代码审查 + 隔离复测）

代码审查：
- 每阶段成功即落盘：`run_stages` 中 `prog.update({"status":"ok",...}); save_state(st)`（runner.py:630-631），stage_progress 每步持久化。
- resume 复用恢复点：`start_run(resume_from=prev)` 沿用原 `stage_progress` 与 `frozen_batch`，不重新冻结输入清单（runner.py:717-724，日志"沿用原输入清单 hash=…（不重新冻结）"）。
- resume 跳过已完成阶段：`if prog["status"]=="ok": log("恢复","阶段 X 已完成，跳过（恢复点生效）"); continue`（runner.py:616-618）。

隔离复测（故障→resume）：注入 gbrain 失败 → brain_refresh failed（依赖，重试 3 次耗尽）→ state=failed；换正常 gbrain 跑 resume：
```
[恢复] resume：沿用原输入清单 hash=sha256:13231322f9be（不重新冻结）与恢复点 {'ingest':'ok','dedup':'ok','brain_refresh':'failed','verify':'pending'}
[恢复] 阶段 ingest 已完成，跳过（恢复点生效）
[恢复] 阶段 dedup 已完成，跳过（恢复点生效）
[恢复] 阶段 brain_refresh 尝试计数重置（新一轮 attempt）
→ brain_refresh ok → verify ok → mechanical_ok
```
attempts 实测：ingest=1（未递增）、dedup=1（未递增）、brain_refresh=1（重置后成功）、verify=1；cards=0 无副本。中断恢复不产生重复（R14）。

### 7. 日志分类审查 — PASS（附说明）

**说明**：最近真实日志（状态机-2026-08-22-sm-20260822T024929Z-a52f.log）为 R7 skip 干净跳过，仅含过程标签 `[调度][锁]`，无故障类标签——此为正确行为（无故障发生不该有故障标签）。DESIGN §2.8 明确区分"故障 7 类"与"过程标签（调度/阶段/锁/状态/恢复）"。

7 类故障标签经隔离受控注入实证：
- `[上游] 源失败（不致命）：ERROR Error fetching source bad-rss: 403 Forbidden` — 单源失败不致命，ingest 仍 ok
- `[网络] 网络类告警：WARNING ProxyError: connection refused`
- `[依赖] 阶段 brain_refresh 失败（第 N 次）：gbrain 退出码 1` — 重试 3 次后 failed
- 其余 4 类（权限/运行时/数据/规则冲突）在代码失败路径中编码：`classify_exception`→运行时/权限/依赖；`stage_dedup/verify`→数据；`stage_verify`→规则冲突（last_closure 不符）。

### 8. 四方一致性 — PASS

R7 skip 复测后实测四方：

| 视图 | 值 | 一致 |
| --- | --- | --- |
| 状态机.json | state=idle, updated_at=2026-08-22T10:49:29, last_success=null | ✅ |
| vault/AGENTS.md 健康块 | `状态机: idle \| 最后成功: 无 \| 更新: 2026-08-22T10:49:29` | ✅ |
| 状态.md 托管行 | `- 状态机: idle（无活动轮次）` | ✅ |
| 状态.md last_closure → 主结案 | `04_系统维护/蒸馏与结案/结案-2026-08-22-轮次1.md`（文件实体存在，status: closed） | ✅ |

注：`last_success=null`（无 mechanical 成功轮）与 `last_closure` 指向第07章手工结案并存——二者是不同概念（前者=状态机机械轮成功，后者=第07章闭环结案），不构成冲突。

### 9. R11 安全审查 — PASS

- **无外发**：无 requests/urllib/http/smtplib/socket 等网络上传库（grep 全空）。3 处 subprocess.run 全为授权机械动作：Horizon 抓取（runner.py:459）、适配器入库（:493）、`gbrain import --no-embed`（:556，本地无 embedding）。
- **无删除**：仅 3 处 unlink，全为自身状态文件——`LOCK_FILE.unlink()`（release_lock，仅当 run_id 匹配，:392）、`STATE_FILE.unlink()`+`LOCK_FILE.unlink()`（uninstall，用户主动，卸载前快照备份，:917-920）。无 os.system/shutil/rmtree/os.remove。
- **不高成本模型**：gbrain 显式 `--no-embed`；finalize_ok 停在 mechanical_ok 并日志"不自动蒸馏/结案，不冒充正式成功"（:773）；蒸馏/路由/结案一律留 agent/用户接力。CARDS_DIR（00_资源库/外部知识）在 runner 中只读（glob/exists），从不写入或删除。

### 10. 不装常驻服务 — PASS

- `crontab -l` → `no crontab for chenzixun`（空）。
- `ls ~/Library/LaunchAgents/` → 8 个 plist 均与状态机无关（ai.openviking.server / claude-to-im / Google / tencent.marvis / tldraw）。
- README §调度策略明确"**不安装 cron/launchd 常驻调度器**"，仅提供可选 crontab 示例供用户自选；理由符合 R6 语义（agent 打开才补跑）。

### 11. 备份存在 — PASS

- `~/.gbrain.backup-20260822/` 存在（gbrain 0.46.25.0 升级前备份，状态.md 记录在案）。
- runner 自身 R13 备份：`vault/04_系统维护/运行记录/状态机-备份-2026-08-22-sm-20260821T201733Z-a2e0/` 含 AGENTS.md + 状态.md 快照（实现前备份）。

### 12. 真实 vault 未被故障测试污染 — PASS

- 00_资源库/外部知识 17 张卡 mtime 全为 02:27 或 03:27（第07章 Horizon 摄取时间），无任何 10:xx（第08章施工时段）新增。
- 00_资源库 中无 test/fake/sm-test/sm-verify/crash/注入 等测试命名文件（grep 无命中）。
- 第08章隔离测试目录 `sm-verify` 与真实 vault 完全分离（/var/folders vs /Users/…/自进化知识库），收尾已删除。
- git status 预期改动甄别：`系统/state_machine/`（新）、`状态机.json`（新）、3 个状态机日志（新）、`AGENTS.md`健康块（改）、`状态.md`托管行（改）均为第08章预期；00_资源库/01_主题Wiki/职业包/结案等系第05/07章产出（基线为第04章提交后）。

## 二、施工方声明核对

| 声明 | 核对结果 |
| --- | --- |
| 966 行纯标准库 | 实测 966 行，import 全标准库 ✅ |
| 6 子命令 | --help 实测 6 个 ✅ |
| flock 单实例锁 | 复测第二实例退出码 3 ✅ |
| 9 状态机 | 全部覆盖（4 驱动 + 5 识别）✅ |
| 恢复点 stage_progress | 落盘+resume 跳过实证 ✅ |
| 故障注入 A-F + R4 全过 | 独立复测 D/F/R4 通过；A（源失败不致命）、B（依赖+resume）经隔离注入实证 ✅ |
| 真实冒烟 force --date 2026-08-22 → R7 skip | 真实复测 skip、无新结案 ✅ |
| 四方一致 | 实测一致 ✅ |
| R11 白名单只做机械阶段 | 无外发/无删除/无高成本模型实证 ✅ |

## 三、最终判定

### **PASS**

12 项审计全部通过，教程 4 条通过标准（唯一结案 / 中断不重复 / 滞后≠故障 / mechanical_ok≠正式成功）均经真实或隔离受控复测实证。施工方声明与独立复测一致，无伪造。

附注（非缺陷，记录备查）：
1. 第 5 项补跑用隔离环境复测（真实 env last_success=null 无补跑可测 + 真实跑会触发网络摄取违反只读边界），场景伪造忠实、语义验证充分。
2. 第 7 项真实日志为干净 skip 仅含过程标签；7 类故障标签经隔离注入实证（DESIGN 明确区分过程标签与故障类）。
3. 锁实现为 flock（设计文 O_EXCL 的正向偏差，已登记）。
