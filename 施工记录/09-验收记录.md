# 第09章验收记录（反馈自进化与质量闸门）

- 验证者：独立验证子智能体（未参与施工，完整报告：施工记录/09-独立验证报告.md）
- 日期：2026-08-22
- 方式：只读审计 + 受控复测（验证员自建 /tmp 迷你 vault 独立构造 8 类坏样本，未复用施工方样本；隔离环境独立复现全链路；真实 vault 仅只读与 review；无外发、无 git 写、无删除、未 kill 进程；验证临时物已自清并复核）
- 输入：教程/09-反馈自进化与质量闸门.md + 施工方声明（施工记录/09-施工报告.md：五节 + T5/T6 小节 + 验证后微修记录）
- 施工经过：首次派发施工停滞零产出被更换；二次派发完成 W1 审计→W2 补缺→W3 测试→T5 真实反馈→T6 隔离全链路→验证后微修
- git 基线：be7d1e0（第01-04章；05/07/08/09 均未提交，逐项甄别）

## 一、逐项审计结果

### 1. 代码存在与可执行 — PASS
quality_gate.py 665 行（check_01~check_15 共 15 项）、weekly_evolution.py 327 行（review/apply）、第09章-质量闸门说明.md 57 行；纯标准库，ast.parse 通过（系统 Python 3.9.6）。

### 2. 教程 14 条闸门映射 — PASS
逐条映射无遗漏无重复：gate3"来源存在、日期有效"拆分为 check_03/04；gate12"用户目标质量高于技术通过"落位新增 check_15（技术通过冒充用户目标→FAIL，仅缺字段→WARN 触发审计，不强制伪升级）。

### 3. T1 真实 vault 全量 — PASS
复跑实测 14 PASS / 0 FAIL / 1 WARN，退出码 0。唯一 WARN=check_10 页面数（实测 .md 73 vs 状态.md 65/65，差 8>2 待刷新，vmc==bp 故不 FAIL）——真实待办非缺陷。索引/embedding 维度因 gbrain serve(PID 34462) 持 PGLite 锁按设计报 N/A 并附进程取证（绝不 kill）。

### 4. T2 坏样本 FAIL — PASS
验证员自建 8 类坏样本全部命中 FAIL 且理由一一对应：password= 长值→②；[[消失的引用源-QA77]]→③；2026/08/26→④；缺 ##判断→⑤；同日同轮双结案→⑧；draft→⑫；2 张否决卡+paused=false→⑬；结案仅"测试通过"无用户目标→⑮。对照组无误报。

### 5. T3/T4 不污染与真实 review — PASS
隔离环境单次反馈卡→review 判"只记录/候选"+"本周无可验证进化"，规则文件 shasum 前后一致；真实 vault review 命中真实反馈卡（n=1 候选）→"本周无可验证进化"，规则与画像+反馈与进化聚合 sha 前后一致=零写入。

### 6. T5 真实反馈链路（诚实性关键项）— PASS
用户真实反馈（2026-08-22 会话原话）落库为 反馈-2026-08-22-结案记录模板.md：原话区经程序化逐字比对=True、status=new、强度=medium 如实声明"非重复次数"。review 判定 n=1 且无 QUANT_RE 口径量化结果数据→候选留观，施工方按指令止步提案之前：未创建提案、未 apply、未改任何规则文件。单次意见未污染全局——以真实数据实证铁律一。

### 7. T6 隔离全链路演示（模拟）— PASS
/tmp/qg-t6：两条标注"模拟"的重复反馈→review 判"可提稳定规则"（n=2）→模拟提案 open→apply --approved（报告注明即模拟批准）→规则文件 version 1→2 且 diff 证实仅此一行增量→提案 accepted→前后对照：旧口径 15 处填写点 vs 新口径 8 处（结论类型+数量清单+证据），-7/-53%。真实 vault 聚合 shasum 三时点一致=零写入。验证员同法独立复现成功（version 1→2、diff 仅一行）。

### 8. 安全边界 — PASS
git status --porcelain -- vault/00_资源库 仅 8 条 ?? 新增（第03章产物），无 M/D 无删除；无外发、无 crontab/launchd 安装、无 kill 进程；状态.md 与 状态机.json 未被本章触碰（mtime 11:08/10:49 早于施工时段 17:51+）。

### 9. 验证后微修 — PASS
验证清单①②③（"14项"措辞→15项口径、docstring 补注 gate12 落位）+ 施工方自查④（argparse help）全部修复；复跑 14 PASS / 0 FAIL / 1 WARN 退出码 0；主智能体独立 grep 确认无残留。

## 二、施工方声明核对（抽样）

| 声明 | 核对 |
| --- | --- |
| 15 项覆盖 14 条闸门 | 映射表逐条核实 ✅ |
| T1 复跑 0 FAIL 退出码 0 | 验证员与主智能体各自独立复跑一致 ✅ |
| 真实反馈卡原话逐字 | 程序化比对 True ✅ |
| T6 零写入 | 聚合 sha 三时点一致+验证员复现 ✅ |
| /tmp 测试残留自清 | 目录确认已清除 ✅ |
| 未触碰状态.md/状态机.json | mtime 早于施工时段 ✅ |

## 三、遗留与待办（不阻断 PASS）

1. **真实进化链路待真实信号**：真实反馈卡候选留观中。待用户确认该意见属"重复"或出现结果数据后，走 提案→用户批准→apply version+1→同类任务前后对照（机制已由 T6+独立复现证明可用，真实链路为纯协议动作）。
2. **check_10 WARN（页面数 73 vs 65）**：待一次 GBrain 刷新消除；状态文件刷新归第02章刷新协议/第08章管辖，需另行授权。
3. **索引/embedding 维度 N/A**：待 gbrain serve 释放窗口经 MCP 工具核验（当前 N/A 已附进程级取证）。
4. **crontab 直查被沙箱拒绝**：以 LaunchAgents 无本项目任务 + 施工方声明为间接证据，不构成缺陷。

## 四、最终判定

### **PASS**

教程验证提示词四项要求全部满足：①完整进化链路——隔离模拟全链路走通并经独立复现（真实链路待真实重复信号，属协议正确行为而非缺陷）；②单次意见不升级全局——以用户本人真实单次反馈实证被正确拦截；③坏样本 FAIL——8 类独立注入全部命中且理由对应；④合格样本 PASS——真实 vault 0 FAIL 退出码 0。施工方在 T5 面对真实反馈未达门槛时止步不造假，纪律执行到位。
