# 01 环境与Obsidian仓库 — 验收记录

- 验证者：独立验证子智能体（未参与施工）
- 日期：2026-08-20
- 方式：只读审计 + 受控测试（临时目录内写入测试，已清理）
- 结论：**CONDITIONAL PASS**（见"最终结论"）

---

## 逐项验证

### 1. Obsidian 来源和版本 — 通过

- `codesign -dv --verbose=4 /Applications/Obsidian.app`：
  - `Identifier=md.obsidian`
  - `Authority=Developer ID Application: Dynalist Inc. (6JSW4SJWN9)`
  - `TeamIdentifier=6JSW4SJWN9`
  - 证书链完整（Developer ID CA → Apple Root CA），确认为 Obsidian 官方（Dynalist Inc.）签名。
- `Info.plist`：`CFBundleShortVersionString=1.13.7`，`CFBundleIdentifier=md.obsidian`。
- vault 注册：`~/Library/Application Support/obsidian/obsidian.json`：
  ```json
  {"vaults":{"50a46f53fb9ca4b7":{"path":"/Users/chenzixun/Documents/自进化知识库/vault","ts":1787216512629,"open":true}}}
  ```
  与施工方声明一致（path 精确匹配、open:true）。

### 2. 双通道读取 安装验证.md — 通过

- 文件系统侧：`cat vault/安装验证.md` 成功，随机测试句逐字匹配：
  > 蓝斑条纹的机械水母在2026年8月20日绕开第三盏灯塔，把七枚回声硬币藏进了潮汐抽屉。
- `stat`：`inode=30629255 size=670 path=/Users/chenzixun/Documents/自进化知识库/vault/安装验证.md`
- Obsidian 侧证据：`vault/.obsidian/workspace.json` 主编辑区叶子节点
  `/main/children[0]/children[0]/state/state/file = "安装验证.md"`（type=markdown, mode=source）；
  反向链接/出链/大纲侧栏 state.file 同样指向 `安装验证.md`。
- 路径一致性：obsidian.json 的 vault 根（`…/自进化知识库/vault`）+ workspace.json 相对路径（`安装验证.md`）拼接 = 文件系统绝对路径，两者指向同一真实路径对象（inode 30629255 仅文件系统侧可观测，Obsidian 无持久 inode 记录，路径同一性即教程要求的核对方式）。

### 3. 绝对路径核对 — 通过

- obsidian.json 中 `path` 与 `/Users/chenzixun/Documents/自进化知识库/vault` 逐字符相等（验证命令以该精确路径 cat/stat 成功）。

### 4. 权限边界测试 — 通过

- `vault/AGENTS.md`（29 行）明确：可写本 vault；vault 外只读；`教程/` 一律只读；授权红线 = 删除、外发（网络上传/邮件/发布）、批量改名、移动 `00_资源库/` 原始资料；禁止提前创建业务目录。
- 工程根 `AGENTS.md`（31 行）明确权限表：`教程/` 禁止修改、`施工记录/` 可写追加式、`vault/` 见 vault 级规则、`系统/` 按章授权；全局安全边界 5 条（含外发/删除/批量改名/数据库迁移需单独授权）。
- 受控写入测试（预批准临时目录 `/var/folders/.../T/opencode`）：
  ```
  第01章验证无害测试文件 1787216658
  WRITE+DELETE OK: .../vrf-ch01-93467.txt
  ```
  写入→读取→删除全部成功，临时目录无残留。
- 行为承诺（写入报告备查）：本次验证未在工程目录外、临时目录外的任何位置创建或修改文件；未修改 `vault/`、`教程/` 下任何文件；未执行 git 写操作；未联网。

### 5. 云同步 / 插件 / remote / 公开发布 — 通过（含 1 个观察项）

- `vault/.obsidian/plugins`：不存在 → 社区插件 0 ✓
- `vault/.obsidian/app.json`：`{}` → 无 sync/publish 相关配置 ✓
- `vault/.obsidian/sync.json`、`publish.json`：均不存在 ✓（未配置同步/发布服务）
- `git remote -v`（工程根）：输出为空，exit 0 → 无 remote ✓
- **观察项（本次 CONDITIONAL 唯一原因）**：`core-plugins.json` 中 `"sync": true`（Sync 核心插件开关为开启；`"publish": false`）。教程检查点 B 要求"不安装社区插件、主题、Sync或Publish"。当前虽无 sync.json、无账号同步配置、无任何实际外发通道，但该开关状态与教程字面要求存在出入，且施工方声明未披露此项。

### 6. 无聊天上下文可理解性 — 通过

仅凭两个 AGENTS.md（不依赖任何会话上下文），验证者可明确回答：
- **我能写哪里**：`vault/`（vault 级 AGENTS.md："agent 可写本 vault"）；`施工记录/`（工程级：可写、追加式）；`系统/` 仅按章授权。vault 之外一律只读，`教程/` 禁止修改。
- **什么需要授权**：删除、外发（网络上传/邮件/发布）、批量改名、批量移动、数据库迁移、secret 写入；`00_资源库/` 原始资料禁止覆盖/移动/删除。

与文档声明一致。

## 额外检查

- vault 根目录仅含 `AGENTS.md`、`安装验证.md`、`.obsidian/`、`.DS_Store`（Finder 自动生成，无害）→ 无业务目录（00_资源库 等），符合"留给第04章" ✓
- `教程/` 目录 11 个文件：00-总导航 + 01~10 全部 10 章，完整 ✓
- 次要观察（不影响本章判定）：工程为 git 仓库但无 `.gitignore`；AGENTS.md 规定"secret 只写被 git 忽略的本地配置"，建议后续章节建立忽略机制。
- `施工记录/进度.md` 中本章状态为"施工中"，与本次验收时点一致。

## 最终结论：CONDITIONAL PASS

**理由**：教程本章通过标准三条（vault 为普通本地 Markdown 目录 / agent 唯一工作根目录明确 / 无覆盖无外发无 secret）全部满足，施工方全部声明均获证据支持；唯一保留项为 `core-plugins.json` 中 Sync 核心插件开关为 `true`，与教程检查点 B"不安装 Sync"字面不符（无实际同步配置或数据外发风险，施工方声明未披露此项）。

**放行条件（二选一）**：
1. 在 Obsidian 设置 → 核心插件中关闭 "Sync" 开关（或由施工方将 `vault/.obsidian/core-plugins.json` 中 `"sync"` 置为 `false`），复验后转 PASS；
2. 用户明确确认接受该默认开关状态（未登录 Obsidian 账号且无 sync.json，无同步行为），记录用户确认后转 PASS。

满足任一条件前，按工程规则不得进入第 02 章施工。

---

## 复验（主智能体，2026-08-20）

- 放行条件执行：`vault/.obsidian/core-plugins.json` 中 `"sync": true` → `false`（Obsidian 默认开启的核心插件开关，从未配置账户；publish 本为 false）。
- 复验：`python3 -c "import json; ..."` 输出 `sync: False | publish: False`。
- 无 sync.json / publish.json / 插件目录 / git remote，与教程"不安装 Sync 或 Publish"一致。

## 最终结论：PASS

- 条件已解除，第01章全部验证项通过。
- 下一章：02-GBrain长期记忆系统。
