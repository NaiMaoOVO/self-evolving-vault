# 02-GBrain长期记忆系统（第二阶段）— 施工验收记录

- 施工者：第02章施工子智能体（第二阶段：数据源注册、导入、检索测试与 MCP 配置）
- 施工时间：2026-08-21 16:41 – 2026-08-22 00:50（中途会话挂起约 8 小时）
- 说明：本记录为施工方自检证据汇总，正式 PASS/FAIL 判定由独立验证子智能体出具
- 环境前置：gbrain 0.46.24.0（第一阶段已装）、PGLite brain schema 132、embedding 禁用、dream/autopilot/外发全关

## 1. 测试数据创建（2026-08-21 16:41–16:42）

vault 新建 3 个测试笔记（均标注"无敏感信息"用途与预期）：

| 文件 | 关键内容 | mtime |
| --- | --- | --- |
| 测试-精确关键词.md | "量子苔藓在低温环境下表现出超导特征" | 16:41:58 |
| 测试-同义语义.md | "某些极端环境下的低等植物，在接近绝对零度时会出现零电阻现象"（与上句零重叠词） | 16:42:02 |
| 测试-冲突陈述.md | "2026-01-15 记录：项目X的预算为50万" + "2026-03-02 记录：项目X的预算为80万" | 16:42:06 |

导入前 vault md 文件数实测：**5**（AGENTS.md、安装验证.md + 新 3 个）✓

## 2. 数据源注册

```
$ gbrain sources add evolving-kb --path "/Users/chenzixun/Documents/自进化知识库/vault" --name "evolving-kb" --force
Created source "evolving-kb" → /Users/chenzixun/Documents/自进化知识库/vault
  federated: false — only searched when explicitly named via --source

$ gbrain sources federate evolving-kb
Source "evolving-kb" is now federated (appears in cross-source default search).
```

- `--force` 偏差说明：官方要求源路径为已提交文件的 git repo；工程根 git 仓库**尚无 commit** 且本阶段禁止 git 操作，故用官方 `--force` 跳过检查（合规、留痕）。
- 追加 federate：使其进入默认搜索（默认 false 会导致日常检索漏掉 vault）。

## 3. 全量导入（2026-08-21 16:42–16:44）

```
$ gbrain import "/Users/chenzixun/Documents/自进化知识库/vault" --no-embed
[gbrain] routing to source 'evolving-kb' (sole non-default source registered; ...)
Found 5 markdown files
[import.files] 5/5 (100%) imported=5 skipped=0 errors=0
Import complete (0.2s): 5 pages imported, 5 chunks created

$ gbrain stats → Pages: 5, Chunks: 5, Embedded: 0  ✓（= 导入前文件数 5）
```

## 4. 检索测试

### 4.1 精确关键词 — 成功 ✓

```
$ gbrain query "量子苔藓"
[3.0116] 测试-精确关键词 -- # 测试-精确关键词 ... 量子苔藓在低温环境下表现出超导
$ gbrain search "量子苔藓"   → 同样命中
```

### 4.2 同义语义 — 局限（如实记录，符合无 embedding 预期）

```
$ gbrain query "低温 低等植物 零电阻"
No results. (degraded: embed_unavailable, keyword_zero)
```

- 降级原因由 gbrain 明示：`embed_unavailable`（用户决定禁用 embedding）。
- 附加诊断：单词检索 `search "低等植物"` / `"零电阻"` / `"绝对零度"` 均正确命中 测试-同义语义.md —— 说明内容已入库，缺的是跨文件语义能力与部分多词组合行为（`search "低等植物 零电阻"` 亦 keyword_zero，而 `search "项目X 预算"` 可命中，行为与默认 FTS 语言 english 的中文分词有关，疑点已记入 02-GBrain配置说明.md 待 embedding 补配后复测）。
- 结论：**当前检索层 = 关键词可用、语义不可用**；启用 embedding 后需复测本项（教程验证提示第 4 条的完整通过依赖该复测）。

### 4.3 冲突查询 — 成功 ✓（两条齐返、未合并）

```
$ gbrain query "项目X 预算" --json
[ { "slug": "测试-冲突陈述", "chunk_text": "... ## 陈述一（较早）\n\n2026-01-15 记录：项目X的预算为50万。\n\n## 陈述二（较晚）\n\n2026-03-02 记录：项目X的预算为80万。...",
    "source_id": "evolving-kb", "evidence": "keyword_exact", ... } ]
```

- 同一返回中含两条各自带日期的陈述，未合并、未择一。
- 检索结果携带 `source_id` 与 `slug`（→ 来源文件路径 测试-冲突陈述.md），满足"结果必须附带来源"的要求。

### 4.4 增量刷新 — 成功 ✓

- 16:45:59 新建 vault/测试-增量.md（含"增量刷新验证句子-ZEPHYR-7749"，文件保留）。
- 增量 import：`Found 6 markdown files；imported=1 skipped=5(unchanged) errors=0`。
- `gbrain stats` → Pages: **6** ✓；`gbrain search "ZEPHYR-7749"` → 命中 测试-增量.md ✓。

## 5. 路径变更探针（受控测试，2026-08-22 00:47）

- 临时目录（预批准 /var/folders/.../T/opencode）建 探针甲.md（MARKER-ALPHA-3311）→ import → 检索命中 ✓。
- 改名 探针乙.md → 再 import → **旧 slug 探针甲残留**，检索双命中（新旧并存）。
- 结论：import 不清理改名残留 → 刷新协议已据此制定"重命名→增量 import→授权→delete 旧 slug→复验"规则。
- 清理：`gbrain delete 探针甲/探针乙`（软删除，72h 可恢复）；`gbrain search MARKER-ALPHA-3311` → No results ✓；Pages 回到 6 ✓。
- 残留观察（如实）：软删后 Chunks 计数停在 8 未回落（硬清除应回落），一致性检查以 Pages 为准。

## 6. opencode MCP 配置（2026-08-22 00:44 前后）

- 偏差：本机无独立 `opencode` CLI（桌面版 OpenCode.app 1.18.18 内嵌运行，PATH/brew/go/bin/cache 均无 CLI 二进制）→ `opencode mcp add` 命令不可用，改用**等效官方配置方式**：直接写 `~/.config/opencode/opencode.json` 的 `mcp` 字段（`opencode mcp add` 的底层效果即编辑此文件；schema 经 opencode 官方配置文档核对）。
- 命令相对模板的稳健性调整：`command` 用绝对路径 `/Users/chenzixun/.bun/bin/gbrain`（~/.bun/bin 不在 GUI 启动进程的 PATH，裸 `gbrain` 会拉起失败）。
- 写入内容（`python3 json.load` 校验通过；原 provider 配置含 apiKey 完整保留、未外泄）：

```json
"mcp": {
  "gbrain": {
    "type": "local",
    "command": ["/Users/chenzixun/.bun/bin/gbrain", "serve", "--surface", "verbs"],
    "enabled": true,
    "environment": { "GBRAIN_HOME": "/Users/chenzixun" }
  }
}
```

- 服务端握手验证（stdin EOF 自动退出，无常驻进程）：

```
initialize → {"serverInfo":{"name":"gbrain","version":"0.46.24.0"},"capabilities":{"tools":{}}}
tools/list → remember / recall / ... （7 个记忆动词工具面）
pgrep "gbrain serve" → 无残留 ✓
```

- 限制说明：`opencode mcp list` 无法执行（无 CLI）；等效验证 = 配置 JSON 合法 + server 握手成功。**新开会话需重启 opencode**（配置不热加载）；教程验证提示第 7 条（新会话经 MCP 检索同一测试句）留待重启后复验。

## 7. doctor / 警告解释（教程验证提示第 10 条）

`gbrain doctor`（2026-08-22，overall 30/100）：分数低主要因类别惩罚，逐条归因：

| 警告 | 解释 | 影响 |
| --- | --- | --- |
| FAIL sync_freshness（evolving-kb never synced） | 指 git 型 `gbrain sync` 从未运行；本工程刷新路线是 `import`（vault 未 git 提交） | 无实际影响，预期状态 |
| WARN brain_score 10/100（embed 0/35, links 0/25, timeline 0/15, orphans 0/15） | embedding 用户禁用；links/timeline/orphan 属第04章知识架构尚未建立 | 阶段性预期，非故障 |
| WARN cycle_freshness（从未 full cycle / dream） | dream/autopilot 用户决定关闭 | 预期 |
| WARN embed_staleness（8 stale chunks） | 全部 chunk 未嵌入（禁用所致） | 语义检索不可用的根因，待授权补配 |
| WARN embedding_column_registry + width（声明 1280 vs 列 1024） | schema pack 遗留声明与列宽不一致 | 启用 embedding 前必须先修 |
| WARN ze_embedding_health + provider_sunset（zeroentropyai:zembed-1，2026-09-04 关停，无 key） | 默认 embedding provider 即将下线 | 启用前必须迁移 provider（候选 voyage / openai） |
| WARN takes_count 0 | takes 功能 opt-in 关闭 | 无影响 |
| WARN skill_preconditions 1 项未满足；retrieval_reflex_health（策略技能未装） | 可选集成未安装 | 不影响本章目标 |

## 8. 交付物清单

- vault/：测试-精确关键词.md、测试-同义语义.md、测试-冲突陈述.md、测试-增量.md（保留）；AGENTS.md 追加 brain-first 协议（见 10）
- 施工记录/：02-GBrain配置说明.md、02-数据源清单.md、02-隐私边界.md、02-刷新协议.md、02-故障分类表.md、02-安装验收记录.md（本文件）
- ~/.config/opencode/opencode.json：mcp.gbrain 条目
- ~/.gbrain/：brain.pglite（pages 6）、sources 注册与 federate

## 9. 边界合规自检

- 未删除/移动 vault 原有文件；未改 教程/ 与工程根 AGENTS.md；无 git 写操作；无外发（无网络上传；仅本地操作）；未输出 secret（opencode 配置中的 apiKey 仅原地保留）。
- 探针测试在预批准临时目录 + brain 内完成，测试页已清理并复验。

## 10. 待复验清单（移交验证子智能体/用户）

1. 重启 opencode 后：MCP 工具可见 + 新会话经 MCP 检索"量子苔藓"/"ZEPHYR-7749"（教程验证提示第 7 条）。
2. embedding 补配后复测：同义语义检索（4.2）、多词中文组合行为、brain_score/embed_staleness 警告消除。
3. 测试-增量.md 保留于 vault，供跨会话复验；未来清理需用户授权。
