# 03-Horizon外部知识系统 — 独立验收记录

- 验证者：独立验证子智能体（未参与施工）
- 验证时间：2026-08-22（只读审计 + 受控测试）
- 依据：教程/03-Horizon外部知识系统.md 验证提示词（9 项）+ 派发任务附加核验（适配器代码审查）
- 验证对象：`~/horizon`（commit c33d733，uv 环境）+ `系统/horizon_adapter.py` + 隔离试跑产物（`$T/horizon-trial`、`$T/horizon-trial-vault`，`$T=/var/folders/3r/fmznmt5s3ws8d93m8z9zwc3m0000gn/T/opencode`）
- 判定：**CONDITIONAL PASS**（理由见文末）

## 逐项结果

### 1. 原始URL/作者/发布时间/抓取时间抽查 — 通过

抽样 9/9（全覆盖隔离 vault 的 9 张卡）：

- **URL 可达性**（curl -sI 走代理 127.0.0.1:7897，200/301/302 计可达）：9/9 可达（8×200 + kagi.com 302）。frontmatter `url` 与正文"来源信息"节一致。
- **frontmatter 齐全**：9 卡均含 `id/title/author/published/url/source_type/source_name/fetched_at/score/tags/sensitivity/source_report` 12 字段，无缺失。
- **作者/发布时间非虚构**（与上游原始数据逐一对照，非仅与日报互对）：
  - HN 5 条直查 Firebase API：`49386659 by ksec 2026-08-21T11:43:38Z`、`49387570 gavide 13:11:28Z`、`49390132 djfergus 16:03:47Z`、`49389565 Edymilson 15:25:42Z`、`49387497 throw7 13:04:31Z` —— 卡片 author/published 与 API 完全一致。
  - RSS 2 条直查 simonwillison.net Atom feed：`stop-making-tuis published 2026-08-21T16:07:32Z`、`matt-webb 15:06:26Z`，feed author=Simon Willison —— 卡片一致。
  - `published`（8-21）与 `fetched_at`（8-22 01:36:42）分字段，无混淆。
- 语义说明：HN 条目的 `author` 为 HN 提交者用户名（日报固有语义，适配器如实转录）；RSS 条目为 feed 名/作者。可追溯，非虚构。

### 2. 时间窗口边界 — 通过

- `.start_ts` = 1787333570 = 本地 2026-08-22 01:32:50（UTC 8-21 17:32:50）。`--hours 6` → 源码 `_determine_time_window`（orchestrator.py:403-405）`since = now(UTC) - 6h` = **UTC 8-21 11:32:50**。
- 日报 9 条 published（UTC，已由 HN API/feed 原始时间证实）区间 11:43–16:07，全部 ∈ [11:32:50, 17:32:50]，**无越界条目**；最早条目 11:43 距窗口起点仅 10 分钟，与 6h 窗口语义自洽。
- 交叉验证：故障注入测试 `--hours 2`（since=UTC 15:41:40）仅产出 16:03、16:07 两条，同样符合窗口。
- 观察项（非缺陷）：日报时间戳为 UTC 但未标注时区；卡片沿用。建议后续在适配器或模板补时区标注。

### 3. 幂等重跑 — 通过（本验证独立执行）

对隔离试跑目录重跑适配器（指向隔离 vault 副本）：

- 输出：`合计: 条目 9 | 新入库 0 | 跳过 9 | 抓取失败 0`，退出码 0
- 卡目录文件数 9 → 9 不变；仅追加运行报告 `Horizon-运行-20260822-014043.md`（追加式产物，保留作证据）
- 施工方隔离 vault 中原有的第二次运行报告（013651：新入库 0/跳过 9）与本验证独立复现一致。

### 4. 无效源故障注入 — 通过（本验证独立执行）

- 临时 config（试跑 config 副本追加 `InvalidTestSource → http://invalid.example.nonexistent/rss`），`--hours 2` 新隔离目录运行：
- 运行日志明确报告：`WARNING Error fetching RSS feed InvalidTestSource: Server error '502 Bad Gateway'`（代理解析失败的表征）——**报告为抓取失败，未写"无新增"**；且整体流程继续，产出日报（2 条目）。
- 其它源正常：HN 1 条 + Simon Willison RSS 1 条入稿；GameLook 不可达、游研社 403 均如实 WARNING，与施工声明一致。
- GitHub 源：`Found 0 items from GitHub`，**无鉴权错误** —— GITHUB_TOKEN 注释后匿名访问已恢复（0 条为窗口内两 repo 无新 release，属正常）。无效源未混入日报（grep=0）。

### 5. AI 失败故障注入 — 架构限制（如实记录）

进程级 `DEEPSEEK_API_KEY=sk-invalid-for-test`（未改 .env，load_dotenv 不覆盖已有环境变量，注入生效）：

- 两个已抓取条目均 `ERROR ... raised AuthenticationError`，`Selected 0 items`，日报为"暂无重要动态"占位文本。
- **原始条目零落盘**：data-dir 仅有 summaries 两份占位日报，无任何含原始条目（标题/URL/时间）的输出。Horizon 不提供 AI 失败时的原始数据保存路径——**架构限制**，适配器以日报为唯一输入，无法补救。
- 附带观察（上游缺陷）：占位日报措辞"信息源较平静/阈值过高"未明示真实原因是 AI 认证失败（仅建议栏第 3 条"检查 AI 模型是否正常工作"间接提示；真实原因只在控制台 ERROR 与 token 用量为 0 中可见）。
- **影响评估**：未伪造成功（无编造摘要/条目），但该窗口数据会静默丢失且日报文本有误导性。不阻断本章验收（教程允许上游不支持时记录限制），但必须移交后续章节：AI 失败需可观测（建议第 08 章状态机纳入 AI 调用失败的监控与告警）。

### 6. 原始与摘要不混写 — 通过

抽查 TigerBeetle、Kagi、Matt Webb 三卡："AI 摘要（Horizon 日报）"与"原文 excerpt"严格分节。excerpt 为 trafilatura 提取的真实页面文本（英文原文，含"Recent articles"导航文本等 AI 不会生成的特征），与中文 AI 摘要无任何混写；抓取失败时 excerpt 节如实写"抓取失败：…"。

### 7. secret 扫描 — 通过

- 工程目录（教程/施工记录/vault/系统）模式扫描（sk- 密钥/赋值型 api_key/password/token）：**0 命中**。
- 两个隔离 vault 副本 + 隔离试跑目录：0 命中。
- `~/horizon/data/*.json`（config.json、config.github.json、presets.json）与 profiles/：无硬编码 key。
- key 仅存在于 `~/horizon/.env`（合法位置，本验证未读取输出任何值）；工程 git status 无 .env 或敏感文件。
- 适配器代码：无任何 secret 硬编码（仅环境变量引用）。

### 8. 未授权外发 = 0 — 通过（附 1 项观察）

- config（主 + 试跑副本）：`webhook.enabled: false`；**无 email 节点**；`ai.base_url` 全库仅 `https://api.deepseek.com` 一处。两次故障注入运行日志无任何邮件/webhook 发送痕迹。实际外部调用 = 源抓取 + api.deepseek.com ✓
- `daily-summary.yml.disabled` 处于禁用 ✓
- **观察项**：上游自带 `deploy-docs.yml`（Pages，on push docs/**）**未加 .disabled**，与施工方"Pages 关闭"声明不符。实际风险为 0，证据链：① 施工方从未 push（`origin/main..HEAD` 为空，HEAD=c33d733=origin/main，且该 remote 为上游公开仓库非施工方 fork）；② orchestrator 复制日报的目标 `docs/_posts/*.md` 被 .gitignore 第 49 行忽略，即使 push 也不会外发日报；③ docs/ 其余内容施工方未改动。判实际外发 0，但建议施工方将 deploy-docs.yml 改名 `.disabled` 或移除该 remote 的 push 权限，消除声明与事实的偏差。

### 9. 停用测试 — 通过

- `pgrep -fl horizon` 无任何进程（命令行工具，无常驻）✓
- 隔离 vault 卡为纯 markdown：cp 至临时路径可完整读取 frontmatter（验证后已删）；不依赖 Horizon 运行时 ✓

### 附加：适配器代码审查 — 通过

- **无外发**：网络仅 `fetch_excerpt` 对条目 URL 做 GET（httpx，经代理 + 无代理重试），无 POST/上传/ telemetry。
- **幂等真实**：`load_existing_ids` 扫描既有卡 frontmatter `id` + 目标文件名 `target.exists()` 双重判断；`item_id = sha256(url)[:12]` 确定性派生——非随机跳过，且经第 3 项实测复证。
- **"抓取失败"路径真实**：fetch 异常/正文提取为空 → 写入"抓取失败：{异常类型}: {详情}"于卡 excerpt 节 → 运行报告独立列 `原文抓取失败` 计数与失败明细。失败事实进入知识卡本体，符合"保留失败事实"。
- 小观察（非缺陷）：同名不同 URL 条目会以 id 后缀重命名避免覆盖（horizon_adapter.py:447-448）。

## 教程通过标准对照

| 标准 | 结果 |
| --- | --- |
| 真实来源可追溯 | ✓ 9/9 URL 可达，作者/时间与上游原始数据逐条一致 |
| 重跑不重复 | ✓ 独立复跑 0 新增 |
| 部分源或 AI 失败不伪造成功 | 部分 ✓：部分源失败如实报错不伪造；AI 失败无编造内容，但原始资料不保留（上游架构限制）且占位措辞有误导性 |

## 结论：CONDITIONAL PASS

9 项验证：8 项通过、1 项架构限制（AI 失败时原始资料不保留，上游 Horizon 无此能力，非施工缺陷，未伪造成功）。secret、外发、幂等、时间边界、可追溯性全部实测通过。

遗留事项（移交后续章节，不阻断本章）：

1. **AI 失败可观测性**（架构限制）：AI 认证/调用失败时该窗口数据丢失且日报措辞误导。建议第 08 章状态机将"AI 失败"纳入监控；或在适配器侧对"已分析 N 条但 0 入选"的日报打警告标记。
2. **deploy-docs.yml 未禁用**（观察项）：实际外发 0（无 push + gitignore 阻断），建议改名 `.disabled` 使声明与事实一致。
3. **时间戳无时区标注**（观察项）：日报/卡片时间为 UTC，建议适配器补注。

验证副作用说明（均已清理/恢复）：幂等重跑在隔离 vault 副本追加 1 份运行报告（追加式产物，保留）；两次故障注入使用了临时 config 与临时数据目录（用后已删）；测试期间覆盖的 `~/horizon/docs/_posts/2026-08-21-summary-{zh,en}.md`（本地 gitignored 文件）已恢复为施工方试跑原内容。真实 vault、系统/horizon_adapter.py、config.json 未被修改。

---

## 收尾处理（主智能体，2026-08-22）

1. **CONDITIONAL #8 修复**：`~/horizon/.github/workflows/deploy-docs.yml` → `.disabled`（与 Pages workflow 同样处理，本地 clone 未 push，实际外发始终为 0）。
2. **CONDITIONAL #5 记录缓解**：AI provider 失败时 Horizon 不落盘原始条目（上游架构限制，占位日报无条目）。缓解：Horizon 时间窗为回溯式，DeepSeek 恢复后以相同/更大 `--hours` 重跑即可完整补回窗口内条目；此限制与补跑流程移交第07/08章运行契约固化。失败不伪造：已验证占位日报不冒充真实内容。
3. **9 卡入 vault**：试跑产物经用户授权覆盖修复（补全真实 excerpt），现存于 `vault/00_资源库/外部知识/`（9 卡）+ `vault/04_系统维护/运行记录/`（运行报告）。
4. **源替换**（用户确认）：GameLook（站点不可达）、游研社（403 反爬）→ 机核 gcores（直连 200）+ GamesIndustry.biz（直连 200）。config.json 已更新并语法校验。
5. **GitHub 源恢复**：GITHUB_TOKEN 占位符注释后匿名抓取正常（验收#4 已证）。

## 最终结论：PASS（含 1 项已记录上游架构限制）

- 教程通过标准"真实来源可追溯、重跑不重复、部分源或AI失败不伪造成功"全部满足。
- 下一章：04-知识架构与首次全量蒸馏。
