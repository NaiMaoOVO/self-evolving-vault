# 优化记录（施工完成后的持续运营台账）

> 十章施工收官后的优化批次登记。原则同施工：诚实、可验证、留痕。

## 批次 A（2026-08-23 发起，09-02 接力执行）

| # | 优化项 | 状态 | 说明 |
| --- | --- | --- | --- |
| A1 | 状态计数刷新 | ✅ 完成(09-02) | `状态.md` 65→104 基线诚实化 → 无锁窗口增量导入后刷至 **107**（+9新页，0错误）；闸门页面数校验：一致(差异2)，WARN 消除 |
| A2 | gbrain schema 类型声明 | ✅ 完成(09-02) | 内置包只读 → fork 私有包 `evolving-kb`（激活生效）；声明 knowledge/output/resource/feedback/evolution-proposal/closure/decision/rule 8 类（含 primitive+prefix 语义映射）；`职业包文件` 中文不合法 slug → 11 文件 frontmatter 规范化为 `career-pack` 后声明；复导入零警告 |
| A3 | gbrain 升级 | ✅ 完成(09-02,有保留) | 先备份 `~/.gbrain.backup-20260902`(62M)；bun update 实装最新 main 提交 055ac6c；tag v0.46.28.0/v0.47.9.0 均因上游 package.json DependencyLoop 无法安装（已留证）——版本串仍显 0.46.25.0 系上游未 bump，功能面 stats/import/search/schema 全部正常 |
| A4 | Horizon 游戏源转正 | ✅ 完成(09-02) | 黄金运行验证过的源合并进 `~/horizon/data/config.json`：新增 游戏陀螺/GameLook/Eurogamer/Rock Paper Shotgun（机核与 GamesIndustry 已在库，去重跳过）；故意坏源排除；原配置备份为 config.json.bak-20260902；JSON 校验通过 |

## 接力收尾（2026-09-02，主智能体）

- 02章复验 PASS 入库（复验人：ZCode 主智能体，快照法跨会话 MCP，方法诚实披露见 02-验收记录 复验记录）；进度表 02 行 CONDITIONAL→PASS
- 修 check_03 断链：两张检索自检卡 `[[测试-*]]` 死链转纯文本+归档注
- 修 check_04：海外商品运营工作台 frontmatter 日期剥离非法后缀
- 收录新项目卡：游戏内容运营AI工作台；记录 4 个测试探针文件既成删除
- 清理 未命名.base（Obsidian Bases 残留）误跟踪
- **闸门终态：PASS=14 / FAIL=0 / WARN=1**（余 WARN 即 A1 的无锁刷新待办）

## 批次 C（2026-09-15）：体检修复 —— 静默漂移与闸门盲区

> 起因：库主委托体检「项目与 Obsidian 联通可优化处」。发现一张真实新卡从未入库且被闸门容差掩盖。
> 完整证据见 `vault/04_系统维护/运行记录/迁移-2026-09-15-学习项目落位.md`。

| # | 项 | 结果 |
| --- | --- | --- |
| C1 | **马原复习计划卡落位迁移** | ✅ 库主明确「属学习项目，不与 vibe coding 工作台混放」→ 新建 `02_项目库/学习项目/` 与 `进行中项目/` 隔离；卡从误落的 `01_主题Wiki/` 迁入；目录名去全角冒号规范化为 `自考-15044马克思主义基本原理`；`type: plan→project`、`status: draft→active`；补齐项目卡七段结构 |
| C2 | **gbrain 增量刷新** | ✅ 4 pages imported / 131 skipped / 0 errors；`gbrain search "15044"` 命中新路径；**旧 slug 无残留**（迁移干净）；`01_主题Wiki` 二级目录回到 3 个（回归上限合规） |
| C3 | **状态计数诚实化** | ✅ `vault_md_count` 142→**141**（磁盘实测）；`brain_pages` 142→**143**（stats 实测）。差额精确查清：+4 张历史测试探针残留（测试-冲突陈述/同义语义/增量/精确关键词，待 72h 硬清除）− 2 张未入库 README，净差 +2 账目闭合 |
| C4 | **`probe_gbrain_cli` 误判 bug 修复** | ✅ 旧逻辑以宽泛关键词（含 `lock`）判定持锁，被 status 正常输出的 `Locks: (none active)` 标题误触发 → CLI 被错误标为不可用，导致索引维度与 check_16 永远无结果。改用精确错误信号判定 |
| C5 | **`check_10` 判据错误修复** | ✅ 旧逻辑 `vmc != bp → FAIL` 语义错误（磁盘 md 数 ≠ 脑内页数，脑含残留本就不等），且 `diff>2` 才 WARN → **±2 容差掩盖了真实漂移**。改为：`vault_md_count` 须与磁盘严格一致（否则 FAIL）；`brain_pages` 差额 ≤ 已知残留数(4) 记 PASS，超出则 WARN |
| C6 | **新增 `check_16` 未入库新卡检测** | ✅ 直接做 brain↔disk 集合差，专捕「磁盘有、脑中无」——即漏掉马原卡的那类漂移。**受控验证**：放置未入库探针卡 → 正确报 WARN 点名文件；探针已清理。CLI 不可用时如实 N/A 不伪造 |
| C7 | **首页假链接修复 + 学习项目入口** | ✅ 资源库行原以单卡链接冒充目录入口（点击跳进不相干单卡）→ 改为纯文本说明 + 明确标注「入口样例」；02_项目库行补入学习项目实链，工作/学习两类清晰分列 |

**批次 C 终态**：闸门 **PASS=15 / FAIL=0 / WARN=1**（唯一 WARN 仍为 check_15 用户目标确认，B 类待库主输入）。

**遗留待授权项**：`.env` 未设 `OBSIDIAN_VAULT_PATH`；`教程/`+`施工记录/` 在 vault 外致 4 条断链；`项目卡模板` 误入库成索引噪音；Obsidian `templates.json` 未配置、`community-plugins.json`+`plugins/` 未入 git；根目录 3 件杂物待清理。

## 批次 B（2026-09-02）：从修好到跑活

| # | 项 | 结果 |
| --- | --- | --- |
| B1 | **状态机生涯首次真实成功轮** | ✅ **里程碑**：ingest（Horizon 9源真实抓取6小时——境外源403重试循环如实记录为不致命）→ 预算熔断停恢复点 → resume 两段式：dedup（69 id，0重复）→ brain_refresh（142页）→ verify → **mechanical_ok**；\`last_success\` 从 None → 2026-09-02；恢复点/预算/不冒充设计全部实战验证 |
| B2 | 首页导航修复 | ✅ 输出库"暂空"→研判报告实链；补两工作台项目卡与工具箱卡入口；资源库计数 17→69 |
| B3 | README 状态表翻正 | ✅ 02 行 CONDITIONAL→PASS |
| B4 | 周进化复查 | ✅ 如实输出"本周无可验证进化"（铁律：无合格信号不伪进化） |

**批次 B 终态**：PASS=14 / FAIL=0 / WARN=1（唯一 WARN 仍为 check_15 用户目标确认）。自动化闭环从"纸面可用"变为"实战已验证"。

## 批次 A 终态（2026-09-02）

**A1-A4 全部完成**。闸门终态：PASS=14 / FAIL=0 / WARN=1（唯一 WARN=check_15 成果用户目标确认，属 B 类待用户输入项）。意外收获：OpenCode 关闭后残留的锁状态自行解除（无文件锁/无 postmaster.pid/无活进程，疑似 PGLite 内部中间态超时自愈）——期间用隔离快照法完成了无害诊断。

## 待窗口触发项（已完成，存档备查）

```bash
# 1) schema 声明（逐类）  2) 增量导入  3) 备份+升级  4) 计数刷新+闸门
export PATH="$HOME/.bun/bin:$PATH"
gbrain schema add-type output && gbrain schema add-type feedback && gbrain schema add-type resource && gbrain schema add-type decision
gbrain import --no-embed && gbrain stats
cp -R ~/.gbrain ~/.gbrain.backup-$(date +%Y%m%d) && gbrain self-upgrade
```
